# انسخ إلى .env.local في نفس المجلد (مستبعد عن Git).
# Settings → Database → Connection string (URI) — غالباً مع كلمة مرور قاعدة البيانات.
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[DB-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# اختياري: من Settings → API → service_role (للاستخدام داخل الدالة فقط، لا للمتصفح)
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# إنتاج: طبقة إضافية — إن وُجد، يجب أن يرسل المتصفح نفس القيمة في رأس x-hse-api-key (انظر Frontend/secrets.local.example.js)
# HSE_API_KEY=ضع_سلسلة_عشوائية_طويلة

# اختياري: لمسار processAIQuestion في الدالة
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
