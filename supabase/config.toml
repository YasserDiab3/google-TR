# Supabase local / CLI config — set project_id after `supabase link`
project_id = "apsawzzqurfnpsucyozb"

[api]
enabled = true
schemas = ["public"]
max_rows = 1000

[functions.hse-api]
verify_jwt = false
